export enum ValidationErrorEnum {
    MinLength = "minlength",
    MaxLength = "maxlength",
    Required = "required",
    Relation = "relation",
    Enum = "enum",
    Min = "min",
    Max = "max",
    Before = "before",
    Chars = "chars",
}
